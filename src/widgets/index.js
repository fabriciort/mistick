/**
 * Widgets Public API
 * 
 * Este arquivo exporta todos os widgets disponíveis.
 * Durante a migração, alguns componentes ainda usam a estrutura legada.
 */

// Widgets Refatorados
export { Header } from './header'
export { Footer } from './footer'

// Widgets ainda na estrutura legada (components/)
// Estes exports serão atualizados conforme a migração progride
// Por ora, re-exportamos da pasta components para manter compatibilidade
